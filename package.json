{
  "name": "auth-spec",
  "version": "0.0.1",
  "private": true,
  "description": "Auth spec, skills, and testing library for Claude Code and beyond",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "bump": "ccbump"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "ccbump": "^0.1.3"
  }
}
